name: Update Weather Data

on:
  workflow_dispatch: {}   # lets you trigger it manually from the Actions tab
  schedule:
    - cron: '*/15 * * * *'   # safety-net check every 15 min; only acts if home data is stale

permissions:
  contents: write
  
concurrency:
  group: weather-data-update
  cancel-in-progress: false

jobs:
  update-weather:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pillow

      - name: Check if home-server weather data is stale
        id: staleness
        run: python scripts/check_staleness.py

      - name: Fetch latest weather data (WU cloud fallback -- only if home data is stale)
        if: steps.staleness.outputs.stale == 'true'
        env:
          WU_STATION_ID: ${{ secrets.WU_STATION_ID }}
          WU_API_KEY: ${{ secrets.WU_API_KEY }}
        run: python scripts/fetch_weather.py

      - name: Fetch WU backup data (always -- feeds dashboard's per-field backfill)
        continue-on-error: true   # supplementary; a WU hiccup here shouldn't block the job
        env:
          WU_STATION_ID: ${{ secrets.WU_STATION_ID }}
          WU_API_KEY: ${{ secrets.WU_API_KEY }}
        run: python scripts/fetch_weather_backup.py

      - name: Fetch latest camera snapshot
        continue-on-error: true   # don't let a camera hiccup block weather data updates
        env:
          ECOWITT_APP_KEY: ${{ secrets.ECOWITT_APP_KEY }}
          ECOWITT_API_KEY: ${{ secrets.ECOWITT_API_KEY }}
          ECOWITT_MAC: ${{ secrets.ECOWITT_MAC }}
        run: python scripts/fetch_camera.py

      - name: Fetch local forecast
        continue-on-error: true   # don't let a forecast hiccup block weather data updates
        run: python scripts/fetch_forecast.py

        
      - name: Fetch air quality
        continue-on-error: true   # don't let an AQI hiccup block weather data updates
        env:
          PURPLEAIR_API_KEY: ${{ secrets.PURPLEAIR_API_KEY }}
        run: python scripts/fetch_air_quality.py

      - name: Fetch current SPC outlook
        continue-on-error: true   # don't let an outlook hiccup block weather data updates
        run: python scripts/fetch_outlook.py

      - name: Fetch active weather alerts
        continue-on-error: true   # don't let an alerts hiccup block weather data updates
        run: python scripts/fetch_alerts.py

      - name: Fetch aurora/Kp data
        continue-on-error: true   # don't let an aurora hiccup block weather data updates
        run: python scripts/fetch_aurora.py
        
      - name: Commit and push if data changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Pull latest remote changes from your local server to avoid push rejections
          git pull origin main --rebase
          
          for f in data/weather.json data/history.json data/weather_wu.json data/camera.jpg data/forecast.json data/outlook-day1.png data/outlook-day2.png data/outlook-day3.png data/air_quality.json data/alerts.json data/aurora.json; do
            [ -f "$f" ] && git add "$f"
          done
          git diff --cached --quiet || git commit -m "Update weather data [skip ci]"
          git push
