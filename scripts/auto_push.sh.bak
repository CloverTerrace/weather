#!/bin/bash
cd /home/weather/weather-site
git pull origin main --rebase --autostash >/dev/null 2>&1
git add data/weather.json data/history.json
if ! git diff --cached --quiet; then
    git commit -m "Auto-update weather data from local WeeWX server [skip ci]"
    git push origin main
fi
